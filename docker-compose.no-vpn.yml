# ==========================================
# TOM SPARK'S PRIVACY BOX (NO VPN MODE)
# Created by Tom Spark | youtube.com/@TomSparkReviews
# ==========================================

services:
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:latest
    container_name: qbittorrent
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
      - WEBUI_PORT=8080
    ports:
      - 8080:8080
    volumes:
      - ${HOME}/PrivacyServer/config/qbittorrent:/config
      - ${HOME}/PrivacyServer/media/downloads:/data/downloads
    restart: always

  prowlarr:
    image: lscr.io/linuxserver/prowlarr:latest
    container_name: prowlarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    ports:
      - 8181:9696
    volumes:
      - ${HOME}/PrivacyServer/config/prowlarr:/config
    restart: always

  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    ports:
      - 8989:8989
    volumes:
      - ${HOME}/PrivacyServer/config/sonarr:/config
      - ${HOME}/PrivacyServer/media:/data/media
      - ${HOME}/PrivacyServer/media/downloads:/data/downloads
    restart: always

  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    ports:
      - 7878:7878
    volumes:
      - ${HOME}/PrivacyServer/config/radarr:/config
      - ${HOME}/PrivacyServer/media:/data/media
      - ${HOME}/PrivacyServer/media/downloads:/data/downloads
    restart: always

  jellyfin:
    image: lscr.io/linuxserver/jellyfin:latest
    container_name: jellyfin
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    ports:
      - 8096:8096
    volumes:
      - ${HOME}/PrivacyServer/config/jellyfin:/config
      - ${HOME}/PrivacyServer/media/tv:/data/tvshows
      - ${HOME}/PrivacyServer/media/movies:/data/movies
      - ${HOME}/PrivacyServer/media/music:/data/music
    restart: always

  # --- SABnzbd (Optional - enable with: docker compose --profile sabnzbd up -d) ---
  sabnzbd:
    image: lscr.io/linuxserver/sabnzbd:latest
    container_name: sabnzbd
    profiles:
      - sabnzbd
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    ports:
      - 8085:8080
    volumes:
      - ${HOME}/PrivacyServer/config/sabnzbd:/config
      - ${HOME}/PrivacyServer/media/downloads:/data/downloads
    restart: always

  # --- Lidarr (Optional - enable with: docker compose --profile lidarr up -d) ---
  lidarr:
    image: lscr.io/linuxserver/lidarr:latest
    container_name: lidarr
    profiles:
      - lidarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    ports:
      - 8686:8686
    volumes:
      - ${HOME}/PrivacyServer/config/lidarr:/config
      - ${HOME}/PrivacyServer/media:/data/media
      - ${HOME}/PrivacyServer/media/downloads:/data/downloads
    restart: always

  # --- Homarr (Optional - enable with: docker compose --profile homarr up -d) ---
  homarr:
    image: lscr.io/linuxserver/homarr:latest
    container_name: homarr
    profiles:
      - homarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    ports:
      - 7575:7575
    volumes:
      - ${HOME}/PrivacyServer/config/homarr:/config
    restart: always

  # --- Notifications (Optional - enable with: docker compose --profile notifications up -d) ---
  notifiarr:
    image: golift/notifiarr
    container_name: notifiarr
    hostname: notifiarr
    profiles:
      - notifications
    environment:
      - DN_API_KEY=${NOTIFIARR_API_KEY}
      - TZ=${TZ}
    ports:
      - 5454:5454
    volumes:
      - ${HOME}/PrivacyServer/config/notifiarr:/config
      - /var/run/utmp:/var/run/utmp:ro
    restart: always

  # --- FlareSolverr (Optional - enable with: docker compose --profile flaresolverr up -d) ---
  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr:latest
    container_name: flaresolverr
    profiles:
      - flaresolverr
    environment:
      - LOG_LEVEL=info
      - TZ=${TZ}
    ports:
      - 8191:8191
    restart: always
